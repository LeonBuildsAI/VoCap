# AGENTS.md

## 语言要求

永远使用中文进行交流，包括代码注释和 AI 生成的 markdown 文本 以及 commit message。

## 核心规则

1. 编译验证：修改代码后必须验证测试编译通过，绝不能跳过
2. 禁止擅自提交：不要主动执行 git commit、git push 等 git 操作，除非用户明确要求
3. bug修复和需求开发禁止在未考虑架构影响和后续扩展的情况下直接修改代码，需要保证考虑完整，从架构合理上解决问题，保证不会出现这一类问题而不是仅仅修复这一个bug
4. 临时文件及时删除，创建文件过多要注意文件目录划分和层次合理整洁
5. 不要在没有经过同意的情况下使用兜底的方式解决问题，要从问题根本上分析，废弃代码功能及时删除
6. subagent-driven-development 实现 spec 过程中注意同步 openspec 对应的 task
7. 不要使用 worktrees 模式，如果是一个新的功能可以开一个新的 feature 分支
8. 当你批量删除文件或文件夹时，一定要有绝对的理由，防止误删
9. 尽可能使用组件库或其他热门库，尽量避免造轮子行为
10. 当用户主动让你 commit 时，需要保证 commit 规范
